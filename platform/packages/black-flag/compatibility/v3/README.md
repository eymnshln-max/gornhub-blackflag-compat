# Black Flag compatibility v3 — high-resolution targets

Extends the v2 render-target filter to width400..4096,height200..2160. Same pinned D3DMetal binary, source-state restoration and SDR/HDR pause workaround. v2 remains the user-confirmed rollback.

High-resolution GPU controls validate the normal (non-test-mode) filter; user confirmed gameplay and selected High graphics at 1920x1200. No claims of performance or full-campaign verification.
